<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 继承本地父项目 (fun.commons.lotask4j:lotask4j-parent) -->
    <parent>
        <groupId>fun.commons.lotask4j</groupId>
        <artifactId>lotask4j-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- ASTS 独立部署样例壳 (issue #4): 壳内零业务代码, 全部业务由
         lotask4j-server-spring-boot-starter 自动装配。仓库内角色 = 独立运行样例 + compose/smoke 联调载体;
         家族接入请建自家薄壳 (1 主类 + 1 yml + 本 starter 依赖)。 -->
    <artifactId>lotask4j-backend</artifactId>
    <packaging>jar</packaging>

    <name>lotask4j 异步慢任务服务 (ASTS) - 独立部署样例壳</name>
    <description>薄壳应用: 引 lotask4j-server-spring-boot-starter 即得完整 ASTS 服务 (:9080)</description>

    <dependencies>
        <!-- ASTS 全量业务 (controller/service/mapper/config + 迁移脚本 + 控制台/embed 静态资源) -->
        <dependency>
            <groupId>fun.commons.lotask4j</groupId>
            <artifactId>lotask4j-server-spring-boot-starter</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- ==================== 测试依赖 ==================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Spring Boot Maven 插件 — repackage 必须显式绑定 (父 POM 非 spring-boot-starter-parent,
                 pluginManagement 不会自动带 repackage 执行; 缺失时 package 产出不可启动的瘦 jar)。
                 mainClass 显式钉死: 壳主类在 .app 子包, 避免启动器猜测。 -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>fun.commons.lotask4j.app.AstsApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>

            <!-- Maven Surefire Plugin (测试插件) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>

            <!-- 装配壳豁免 JaCoCo 门禁 (家族惯例, 参 token-gateway starter): 壳内零业务行,
                 覆盖率门禁随业务代码落在 starter 模块 -->
        </plugins>
    </build>
</project>
